# Live Video to ASCII Converter
A real-time, high-performance web application that intercepts a live webcam feed and mathematically converts the pixel brightness data into an ASCII character grid using the HTML5 Canvas API and React.

## How to Run
1. Navigate to the client folder: `cd client`
2. Install dependencies: `npm install`
3. Start the dev server: `npm run dev`